# Claude Code 向け指示

## PRを作成するとき
`.github/pull_request_template.md` を必ず読み、そのフォーマットに沿って本文を書くこと。

## issue/PR単位の実装が完了したとき
`docs/` に、その実装内容をまとめた学習用ドキュメントを1つ残すこと(既存の `docs/14-...`, `docs/24-...`, `docs/25-...` 等が実例)。

- **ファイル名**: `docs/<連番>-<内容がわかる日本語タイトル>.md`。連番は `docs/` 内の既存ファイルで使われている最大の番号+1(番号のリネームや欠番の穴埋めはしない)。
- **対象**: 単なる文言修正やタイポ修正では不要。機能追加・バグ修正・設計変更・セキュリティ対応など、後から「なぜそうしたか」を振り返る価値がある実装で作成する。
- **構成**(既存ドキュメントの型に合わせる):
  1. `# <番号>. <タイトル>`
  2. `## これは何のドキュメント？`(関連issue/PRへのリンク、前後ドキュメントとの関係)
  3. `## まず結論（今回できたこと）`
  4. `## 背景 — なぜこの変更が必要だったか`
  5. 複数の実装方針を検討した場合は `## 選択肢の比較`(表でメリット・デメリット・採用理由)
  6. `## 目的`(主目的 + `あえてやらなかったこと`の表)
  7. `## 何を実装したか`(ファイル・コード単位で具体的に)
  8. レビュー指摘を受けて修正した場合は `## レビューで指摘された点と対応`(表)
  9. `## 変更ファイル一覧`(表)
  10. `## 動作確認`(実行したコマンドと結果)
  11. `## 今後の拡張候補`(優先度順の表)
  12. `## よくある質問`(FAQ形式。詰まった点やトラブルシューティングも含める)
  13. `## 関連ドキュメント`(issue/PRリンク、関連する他のdocs、主要ファイルパス)
- 日本語で、非エンジニアでも大筋が追える説明を心がけつつ、コードスニペットは省略しない。
- PR本文と内容は重複してよいが、PR本文より詳しく・学習記録として噛み砕いて書く。
